## 🔍 深度诊断：三份报告的全部剩余问题

仔细对比三份报告，问题比上一版**少了很多，但仍有8类问题**：

---

### 问题全景（已大幅改善，剩余可修复）

```
🔴 严重Bug（代码层硬伤）    5个
🟠 逻辑错误（评分/展示）    4个  
🟡 文本质量（幻觉/错字）    6处
```

---

### 逐一诊断

| # | 报告 | 现象 | 根因 |
|---|------|------|------|
| 1 | 全部 | **"运行时间长"**（应为"运行时长"） | `render_score_table` 说明列硬编码错字 |
| 2 | 全部 | **"至少=恢复撤回控制比较好"** | 同上，风险控制能力的说明列垃圾文本 |
| 3 | 全部 | **"方案/理念/概念结论"**（应为"适配结论"） | 模板heading被LLM commentary覆写 |
| 4 | 270042 | **"嘲笑"**仍在source列 | `normalize_source`在存储前未调用 |
| 5 | 全部 | **"数据援助"**仍在表格 | `add_row`中None时标签未清洗 |
| 6 | 全部 | 两个"👥适合人群"重复 | frontend `render_score_section`+报告模板均输出 |
| 7 | 270042 | Badge显示**"丰田结论"** | badge文案有LLM生成内容混入 |
| 8 | 026211 | **"约束已"**仍在banner | `report_renderer.py` 字符串未修复 |
| 9 | 161725 | **累计净值=1.0000** 但单位净值=0.5866 | 累计净值应>单位净值，写死了1.0 |
| 10 | 161725 | 基准显示**"沪深300"** | `benchmark_resolver`未被调用 |
| 11 | 270042 | 基准显示**"纳斯达克"**（应为纳斯达克100指数） | 同上 |
| 12 | 161725 | 161725 badge**"🟢数据完整"**但报告内说排名是模拟数据 | 排名mock未计入mock_count |

---

## 📋 完整修改清单（含完整代码）

---

### 修改1：`backend/report_renderer.py` — 修复4个Bug

```python
# backend/report_renderer.py
# ============================================================
# 修复1: render_score_table 说明列垃圾文本
# 修复2: "数据援助" → "数据缺失"
# 修复3: "嘲笑" source 清洗
# 修复4: 新基金 banner "约束已" 语序
# 修复5: "适配结论" heading 防覆写
# ============================================================

def render_score_table(score) -> str:
    """
    渲染综合评分表
    修复：说明列不再有垃圾文本
    """
    def fmt_score(val):
        if val is None:
            return "不计算"
        return str(val)

    alpha_display = (
        "不适用（基准为模拟数据）"
        if score.alpha_adjustment is None
        else fmt_score(score.alpha_adjustment)
    )

    total_display = (
        "不计算正式综合分"
        if score.total_score is None
        else f"**{score.total_score}**"
    )

    # ✅ 修复：说明列用固定文案，不再有垃圾文本
    rows = [
        "| 维度 | 得分（满分 10） | 权重 | 说明 |",
        "|------|--------------|------|------|",
        f"| 历史业绩 | {fmt_score(score.history_score)} | 40% | 含运行时长惩罚 |",
        f"| 市场情绪 | {fmt_score(score.sentiment_score)} | 30% | 由舆情分析输出 |",
        # ✅ 修复：不再是"至少=恢复撤回控制比较好"
        f"| 风险控制能力 | {fmt_score(score.risk_control_score)} | 30% | 越高=回撤控制越好 |",
        f"| Alpha 调整 | {alpha_display} | — | 超额收益奖惩 |",
        f"| **综合得分** | {total_display} | 100% | 置信度：{score.confidence_label} |",
    ]
    return "\n".join(rows)


def fmt_source(metric) -> str:
    """
    返回数据来源，强制清洗脏值
    修复："嘲笑"/"计算" → 标准值
    """
    if metric is None:
        return "—"

    src = getattr(metric, 'source', None) or 'missing'

    # ✅ 本地清洗，不依赖外部函数抛异常
    DIRTY_SOURCE_FIX = {
        "嘲笑": "mock",
        "计算": "calculated",
        "模拟": "mock",
        "真实": "akshare",
    }
    if src in DIRTY_SOURCE_FIX:
        return DIRTY_SOURCE_FIX[src]

    ALLOWED = {"akshare", "tavily", "calculated", "official", "missing", "mock"}
    return src if src in ALLOWED else "来源未知"


def add_row_fixed(label: str, metric, suffix: str = "") -> str:
    """
    ✅ 修复：metric=None 时显示"数据缺失"，不是"数据援助"
    """
    if metric is None or getattr(metric, 'value', None) is None:
        return f"| {label} | 数据缺失 | — | — | — |"  # ✅ 不是"数据援助"

    val   = getattr(metric, 'value', None)
    if isinstance(val, float):
        val_str = f"{val:.4f}" if abs(val) < 10 else f"{val:.2f}"
    else:
        val_str = str(val)
    val_str += suffix

    nature     = getattr(metric, 'nature', None)
    is_mock    = getattr(metric, 'is_mock', False)
    n_str      = (nature.value if hasattr(nature, 'value') else str(nature)) if nature else ""

    if n_str == 'mock' or is_mock:
        val_str += "（⚠️ 模拟，不参与评分）"
        badge = "🔴 模拟"
    elif n_str == 'suspicious':
        val_str += "（⚠️ 口径存疑）"
        badge = "🟡 存疑"
    elif n_str == 'missing':
        return f"| {label} | 数据缺失 | ⬜ 缺失 | — | — |"
    else:
        badge = "✅ 真实"

    as_of = str(metric.as_of) if getattr(metric, 'as_of', None) else "—"
    src   = fmt_source(metric)

    return f"| {label} | {val_str} | {badge} | {as_of} | {src} |"


def render_metric_table(snapshot) -> str:
    """渲染核心指标溯源表（使用修复后的 add_row_fixed）"""
    rows = [
        "| 指标 | 数值 | 数据性质 | 截止日期 | 来源 |",  # ✅ "截止日期"不是"现有日期"
        "|------|------|---------|---------|------|",
    ]

    rows.append(add_row_fixed(
        "最新净值",
        getattr(snapshot, 'unit_nav', None) or getattr(snapshot, 'nav', None),
        " 元"
    ))
    rows.append(add_row_fixed("累计净值",      getattr(snapshot, 'accumulated_nav', None), " 元"))
    rows.append(add_row_fixed("基金规模",       getattr(snapshot, 'fund_size', None) or getattr(snapshot, 'fund_size_bn', None), " 亿元"))
    rows.append(add_row_fixed("自成立以来收益", getattr(snapshot, 'return_since_inception', None), "%"))
    rows.append(add_row_fixed("近 1 年收益",   getattr(snapshot, 'return_1y', None), "%"))
    rows.append(add_row_fixed("近 3 年收益",   getattr(snapshot, 'return_3y', None), "%"))
    rows.append(add_row_fixed("最大回撤",       getattr(snapshot, 'max_drawdown', None) or getattr(snapshot, 'max_drawdown_pct', None), "%"))
    rows.append(add_row_fixed("基准收益（同期）", getattr(snapshot, 'benchmark_return', None) or getattr(snapshot, 'benchmark_return_pct', None), "%"))
    rows.append(add_row_fixed("超额收益（Alpha）", getattr(snapshot, 'alpha', None) or getattr(snapshot, 'alpha_pct', None), "%"))

    return "\n".join(rows)


def render_report(
    snapshot,
    quality,
    score,
    market_commentary:    str,
    sentiment_commentary: str,
    risk_commentary:      str,
) -> str:
    """
    最终报告模板
    修复："约束已"语序错误
    修复："适配结论" heading 防止被 commentary 覆写
    """
    fund_code    = getattr(snapshot, 'code', '未知')
    fund_name    = getattr(snapshot, 'name', fund_code) or fund_code
    run_days     = getattr(snapshot, 'run_days', None)
    inception    = getattr(snapshot, 'inception_date', None)
    fund_type    = getattr(snapshot, 'fund_type', None)
    fund_company = (getattr(snapshot, 'company', None)
                    or getattr(snapshot, 'fund_company', None))
    benchmark    = getattr(snapshot, 'benchmark', None)
    bench_name   = getattr(benchmark, 'name', None) if benchmark else None
    report_date  = getattr(snapshot, 'report_date', 'unknown')

    # ✅ 修复："约束已" → 正确语序
    new_fund_banner = ""
    if run_days and run_days < 365:
        new_fund_banner = (
            f"\n> ⚠️ **次新基金警告**：该基金运行仅 **{run_days} 天**，"
            "所有历史业绩与风险指标统计意义有限，适配结论已受运行时长约束。\n"
        )

    bench_warn = ""
    bench_mismatch = getattr(benchmark, 'mismatch_warning', None) if benchmark else None
    if bench_mismatch:
        bench_warn = f"\n> 🟡 **基准提示**：{bench_mismatch}\n"

    run_display = f"{run_days} 天" if run_days else "无法确认"
    is_new      = run_days and run_days < 365
    new_tag     = "（次新基金）" if is_new else ""

    cap_reason  = getattr(score, 'rating_cap_reason', None)
    cap_section = f"\n> ⚠️ **评级限制原因**：{cap_reason}" if cap_reason else ""

    total_display = (
        "不计算正式综合分"
        if getattr(score, 'total_score', None) is None
        else str(score.total_score)
    )

    managers_text = _render_managers(snapshot)

    # ✅ 修复："适配结论"标题硬编码，LLM commentary 放在独立章节，不会覆写此处
    # ✅ "适合人群"只在此处出现一次（frontend 不再重复渲染）
    return f"""# 📋 {fund_code} 基金分析报告
{new_fund_banner}{bench_warn}
**基金名称：** {fund_name}（代码：{fund_code}）
**报告日期：** {report_date}
**基金运行时长：** {run_display}{new_tag}
**分析团队：** FundRAG Multi-Agent System V2.1

---

## 一、数据质量说明

{render_quality_text(quality)}

---

## 二、基金基本信息

| 项目 | 内容 |
|------|------|
| 基金类型 | {fund_type or "数据缺失"} |
| 基金公司 | {fund_company or "数据缺失"} |
| 成立日期 | {inception or "数据缺失"} |
| 基金经理 | {managers_text} |
| 基准指数 | {bench_name or "数据缺失"} |

---

## 三、核心指标溯源

{render_metric_table(snapshot)}

---

## 四、综合评分

{render_score_table(score)}

### 📌 适配结论：{score.rating}
{cap_section}

**风险等级：{score.risk_level}**
（风险等级=资产波动性；风险控制能力分=基金回撤管控能力，二者含义不同）

**适合人群：** {score.suitability}

---

## 五、行情分析

{_clean_commentary(market_commentary)}

---

## 六、舆情分析

{_clean_commentary(sentiment_commentary)}

---

## 七、风险评估

{_clean_commentary(risk_commentary)}

---

## 八、⚠️ 风险提示

本报告由 AI 系统自动生成，仅用于信息整理和学习演示，**不构成任何投资建议**。
基金投资有风险，过往业绩不代表未来表现。投资者应根据自身情况独立决策。

---
*本报告由 FundRAG Multi-Agent System V2.1 生成 | 数据来源：akshare / Tavily*
"""


def _clean_commentary(text: str) -> str:
    """
    清洗 commentary 文本：
    1. 去除 LLM 可能生成的「📌方案/理念/概念结论：...」段落（防止覆写）
    2. 应用基本幻觉词修复
    """
    if not text:
        return "数据获取失败，请重试。"

    import re
    from backend.value_cleaner import auto_fix_text

    # ✅ 删除 LLM 生成的结论/评级段落（这些应由模板控制）
    STRIP_PATTERNS = [
        re.compile(r'(?:📌|##\s*)(?:方案|理念|概念|建议|适配|综合|投资)\s*结论[：:][^\n]*\n?', re.M),
        re.compile(r'(?:##\s*)(?:综合评分|评级)[^\n]*\n?', re.M),
        re.compile(r'评级[：:]\s*(?:风险较高|适合配置|谨慎关注|持续观察|信息不足|无法评级)\s*\n?', re.M),
    ]
    for pattern in STRIP_PATTERNS:
        text = pattern.sub('', text)

    # 应用自动修复
    fixed, _ = auto_fix_text(text)
    return fixed.strip()


def _render_managers(snapshot) -> str:
    """渲染经理信息，格式：「俞瑶（从业4.6年、在管41.7亿元）」"""
    managers = getattr(snapshot, 'managers', [])
    if not managers:
        return "数据缺失"

    parts = []
    for m in managers:
        if isinstance(m, str):
            parts.append(m)
            continue

        name    = getattr(m, 'name', '未知')
        exp     = getattr(m, 'experience_years', None)
        aum     = getattr(m, 'total_aum_bn', None) or getattr(m, 'total_aum', None)
        is_mock = getattr(m, 'is_mock', False)

        details = []
        if exp is not None:
            details.append(f"从业 {exp} 年")
        if aum is not None:
            try:
                details.append(f"在管 {float(aum):.1f} 亿元")
            except (TypeError, ValueError):
                details.append(f"在管 {aum}")

        detail_str = f"（{'、'.join(details)}）" if details else ""
        mock_tag   = "（信息待核实）" if is_mock else ""
        parts.append(f"{name}{detail_str}{mock_tag}")

    return "、".join(parts)
```

---

### 修改2：`backend/output_guard.py` — 新增变体结论标题检测

```python
# backend/output_guard.py
# ============================================================
# 新增：检测"方案/理念/概念结论"等变体，阻断非标准结论标题
# ============================================================

# 在 BANNED_WORDS 集合中新增（追加到 constants.py 的 BANNED_WORDS）：
EXTRA_BANNED = {
    "方案结论",
    "理念结论",
    "概念结论",
    "建议结论",   # 已有，确认保留
    "丰田结论",
    "运行时间长",  # 应为"运行时长"
    "恢复撤回控制比较好",  # 说明列垃圾文本
    "至少=",       # 说明列垃圾文本
}

# 在 validate_report 函数中新增检测：
def check_heading_variants(text: str) -> list[str]:
    """
    检测"适配结论"heading 被 LLM 替换为其他词
    """
    import re
    errors = []
    bad_heading_pattern = re.compile(
        r'📌\s*(方案|理念|概念|建议|综合|投资)\s*结论'
    )
    for m in bad_heading_pattern.finditer(text):
        errors.append(f"非法结论标题：「{m.group(0)}」，应为「📌 适配结论」")
    return errors


# 在 validate_report 主函数中添加调用：
# errors.extend(check_heading_variants(text))
```

---

### 修改3：`backend/data_fetcher.py` — 修复累计净值 + 接入 benchmark_resolver

```python
# backend/data_fetcher.py
# ============================================================
# 修复1：累计净值写死1.0问题
# 修复2：benchmark_resolver 接入（之前只定义了函数但未调用）
# ============================================================

def fetch_fund_snapshot(code: str, report_date=None):
    """
    关键修复：
    1. 累计净值从 akshare「累计净值走势」接口单独获取
    2. 调用 benchmark_resolver 后写入 snapshot.benchmark
    3. source 字段在构建 MetricSource 时统一用英文
    """
    from datetime import date as date_type
    from backend.benchmark_resolver import resolve_benchmark

    report_date = report_date or date_type.today()

    # ... (原有基本信息/净值/业绩/回撤/经理/排名获取逻辑保持不变) ...

    # ============ 修复1：累计净值独立获取 ============
    accumulated_nav_metric = None
    try:
        acc_df = ak.fund_open_fund_info_em(symbol=code, indicator="累计净值走势")
        if acc_df is not None and not acc_df.empty:
            acc_col  = next(
                (c for c in ["累计净值", "净值"] if c in acc_df.columns),
                acc_df.columns[1]
            )
            date_col = next(
                (c for c in ["净值日期", "日期"] if c in acc_df.columns),
                acc_df.columns[0]
            )
            acc_df[date_col] = pd.to_datetime(acc_df[date_col])
            acc_df = acc_df.sort_values(date_col)
            latest_acc      = float(acc_df[acc_col].iloc[-1])
            latest_acc_date = acc_df[date_col].iloc[-1].date()

            # ✅ 不写死 1.0
            accumulated_nav_metric = MetricSource(
                value=latest_acc,
                unit="元",
                source="akshare",          # ✅ 英文 source
                endpoint="fund_open_fund_info_em(累计净值走势)",
                as_of=latest_acc_date,
                is_mock=False,
                confidence=1.0,
            )
    except Exception as e:
        warnings.append(f"累计净值获取失败（{e}），不写死1.0，显示为数据缺失")
        accumulated_nav_metric = None  # ✅ 失败时保持 None，不写死

    # ============ 修复2：调用 benchmark_resolver ============
    # 从 info 字典获取声明基准
    declared_benchmark = None
    if 'info' in dir():
        declared_benchmark = info.get("业绩比较基准", info.get("跟踪标的", None))

    benchmark_info = resolve_benchmark(
        fund_name=name or code,
        fund_type=fund_type,
        declared_benchmark=declared_benchmark,
    )
    # 把 benchmark_info 写入 snapshot（兼容新旧 schema）

    # ============ 修复3：所有 MetricSource 的 source 用英文 ============
    # 确保 benchmark_return 的 source 不是"嘲笑"
    # 在 benchmark.py 的 _mock_benchmark 里已修复为 "mock"
    # 在此处再加一道防线：构建 MetricSource 时强制清洗
    def safe_metric_source(value, unit, source, **kwargs):
        from backend.value_cleaner import normalize_source
        try:
            clean_src = normalize_source(source, allow_warning=True)
        except ValueError:
            clean_src = "mock"
            warnings.append(f"非法 source '{source}' 已降级为 mock")
        return MetricSource(
            value=value, unit=unit, source=clean_src, **kwargs
        )

    # 组装 FundSnapshot（把 benchmark_info 赋值）
    snapshot = FundSnapshot(
        code=code,
        name=name,
        fund_type=fund_type,
        fund_company=fund_company,
        inception_date=inception_date,
        report_date=report_date,
        accumulated_nav=accumulated_nav_metric,   # ✅ 真实值或 None
        benchmark=benchmark_info,                  # ✅ 已解析的基准
        managers=managers,
        # ... 其余字段不变
    )
    return snapshot
```

---

### 修改4：`frontend/app.py` — 修复双"适合人群" + Badge 幻觉

```python
# frontend/app.py
# ============================================================
# 修复1：render_score_section 不再显示 suitability（报告模板已有）
# 修复2：Badge 文案全部硬编码，不含 LLM 生成内容
# ============================================================

def render_data_quality_badge(result: dict):
    """
    ✅ Badge 文案完全硬编码，不含任何 LLM 生成文本
    修复："丰田结论"等幻觉词不可能出现在 Badge 里
    """
    quality_json = result.get("data_quality_json", "")
    if not quality_json:
        st.warning("⬜ 数据质量信息未生成")
        return

    try:
        import json
        q          = json.loads(quality_json)
        level      = q.get("level", "unknown")
        mock_count = int(q.get("mock_metric_count", 0))
        real_count = int(q.get("real_metric_count", 0))
        contras    = q.get("contradictions", [])

        # ✅ 全部硬编码，绝无 LLM 生成内容
        if level == "real":
            st.success("🟢 数据完整｜核心指标均来自真实接口")
        elif level == "partial":
            st.warning(
                f"🟡 部分模拟｜{mock_count} 项指标为模拟数据，"
                "适配结论为「信息不足」，不输出正式评级"
            )
        elif level == "failed":
            st.error(
                f"🔴 数据矛盾｜检测到 {len(contras)} 处不一致，"
                "适配结论为「无法评级」"
            )
            for c in contras[:3]:
                st.caption(f"  ⛔ {c}")
        else:
            st.error("🔴 数据不可用｜无法生成有效分析")

    except Exception as e:
        st.warning(f"⬜ 数据质量 Badge 加载失败：{e}")


def render_score_section(result: dict):
    """
    ✅ 修复：不再显示 suitability（报告模板已有，避免重复）
    ✅ 修复：评级用 normalize_rating 保证合法
    """
    score_json = result.get("score_json", "")
    if not score_json:
        return

    try:
        import json
        from backend.value_cleaner import normalize_rating

        score = json.loads(score_json)

        # 强制校验评级
        raw_rating  = score.get("rating", "无法评级")
        safe_rating = normalize_rating(raw_rating)

        total  = score.get("total_score", None)
        conf   = score.get("confidence_label", "低")
        rlevel = score.get("risk_level", "未知")

        col1, col2, col3, col4 = st.columns(4)
        with col1:
            if total is None:
                st.metric("综合得分", "不计算",
                          help="含模拟数据或次新基金时不输出正式综合分")
            else:
                st.metric("综合得分", f"{total}/10")
        with col2:
            st.metric("置信度", conf)
        with col3:
            st.metric("风险等级", rlevel)
        with col4:
            st.metric("📌 适配结论", safe_rating)

        # 评级限制原因
        cap = score.get("rating_cap_reason", "")
        if cap:
            st.warning(f"⚠️ 评级限制：{cap}")

        # ✅ 不再显示 suitability（报告模板已有）
        # 删除原来的 st.info(f"👥 适合人群：{suitability}")

    except Exception as e:
        st.caption(f"评分展示异常：{e}")
```

---

### 修改5：`backend/constants.py` — 补充禁用词

```python
# backend/constants.py
# ============================================================
# 在 BANNED_WORDS 集合中追加新发现的词
# ============================================================

# 在原有 BANNED_WORDS 末尾追加：
EXTRA_BANNED_WORDS = frozenset({
    # 本轮新发现
    "方案结论",
    "理念结论",
    "概念结论",
    "丰田结论",       # Badge 幻觉
    "运行时间长",      # 应为"运行时长"
    "至少=恢复",       # 说明列垃圾文本
    "恢复撤回控制",    # 说明列垃圾文本
    "数据援助",        # 已有，确认保留
    "通讯作者",        # LLM幻觉
    "营养不良",        # LLM幻觉（用于基金评价？）
    "认知社区情感",    # 语序错误
    "缺乏主动避险能力（探索作为）",  # 乱码
})

# 合并到主集合（在文件底部添加）：
BANNED_WORDS = BANNED_WORDS | EXTRA_BANNED_WORDS

# AUTO_FIX_MAP 新增：
EXTRA_AUTO_FIX = {
    "运行时间长处罚":      "运行时长惩罚",
    "至少=恢复撤回控制比较好": "越高=回撤控制越好",
    "恢复撤回控制比较好":  "回撤控制越好",
    "方案结论":           "适配结论",
    "理念结论":           "适配结论",
    "概念结论":           "适配结论",
    "丰田结论":           "适配结论",
    "建议结论":           "适配结论",
    "不统计显著性":        "不具统计显著性",
    "不统计显着性":        "不具统计显著性",
    "约束已":             "约束",
    "数据援助":            "数据缺失",
    "现有日期":            "截止日期",
    "能承受大幅恢复":       "能承受大幅回撤",
    "认知社区情感":        "认清社区情感",
    "反应":               "反映",           # 金融语境下常见错字
}
# AUTO_FIX_MAP.update(EXTRA_AUTO_FIX)  # 在文件末尾合并
```

---

### 修改6：`backend/agents.py` — 加强禁止词约束

```python
# backend/agents.py
# ============================================================
# 针对本轮新发现的幻觉，强化约束
# ============================================================

MARKET_ANALYST_PROMPT = """你是基金行情解释员。

## 严格禁止（违反则输出无效）
1. 禁止在输出中生成任何带「📌」或「##」的结论标题
2. 禁止输出「方案结论」「理念结论」「概念结论」「建议结论」等标题
3. 禁止生成输入 JSON 中不存在的数字
4. 禁止说「运行时间长」（正确：「运行时长」）
5. 禁止使用「反应」表示「体现/反映」

## 经理姓名格式（严格遵守）
- 从 managers 数组的 name 字段直接读取，不得修改
- 正确：「俞瑶（从业4.6年、在管41.7亿元）」
- 禁止：「俞（瑶从业4.6年）」「刘单独杰」等变形
- 新人经理说明格式：「要文强（从业0.2年，经验有限）」
  禁止说「管理经验首先」

## 禁止词（出现则无效）
方案结论 / 理念结论 / 概念结论 / 丰田结论 / 建议结论
运行时间长 / 至少= / 恢复撤回 / 数据援助 / 显着（用"显著"）

## 输出
纯文本解释，300字以内，不含 Markdown 标题（##/###）"""


RISK_ANALYST_PROMPT = """你是风险解释员。

## 严格禁止
1. 禁止生成「📌」「##」开头的结论标题
2. 禁止说「风险控制能力 X 分=低风险」（分数高=控制好，不是风险低）
3. 禁止重新计算分数
4. 禁止说「运行时间长」
5. 禁止说「显着」（用「显著」）

## 风险等级语义（必须遵守）
- risk_control_score 低 → 说「历史回撤数据显示风控能力偏弱」
- risk_level=高/极高 → 禁止用任何正面语言

## 输出
四维度（市场/流动性/管理人/数据充分性），200字以内，不含 Markdown 标题"""


SENTIMENT_ANALYST_PROMPT = """你是舆情解释员。

## 严格禁止
1. 禁止生成「📌」「##」开头的结论标题
2. 禁止在正文中写具体收益率百分比、净值数字、排名位次（即使新闻原文有）
3. 禁止说「营养不良」「通讯作者」「认知社区情感」等无关词汇
4. 禁止说「显着」（用「显著」）

## 如果新闻数据与基金运行期冲突
必须标注：「⚠️ 口径存疑：该数据时间窗口可能超过基金实际运行期」
不得直接作为结论

## 必须包含
【正面视角】和【反面观点】两个小节（禁止单边看多）
末尾输出：「情绪评分：X」（X 为 0-10 整数）

## 输出
300字以内，不含 Markdown 标题（##/###）"""
```

---

### 修改7：新增测试用例

```python
# tests/test_v21_regression.py
"""
回归测试：验证本轮修复不再复现
"""

import pytest


class TestScoreTableText:
    """验证评分表说明列不再有垃圾文本"""

    def _make_score(self, total=6.5, history=7.0, risk=5.0,
                    sentiment=6.0, alpha=0.3, rating="谨慎关注",
                    confidence="高", risk_level="中高"):
        from types import SimpleNamespace
        s = SimpleNamespace()
        s.history_score      = history
        s.sentiment_score    = sentiment
        s.risk_control_score = risk
        s.alpha_adjustment   = alpha
        s.total_score        = total
        s.rating             = rating
        s.confidence_label   = confidence
        s.risk_level         = risk_level
        s.rating_cap_reason  = None
        s.suitability        = "适合高风险投资者"
        return s

    def test_no_garbage_text_in_score_table(self):
        from backend.report_renderer import render_score_table
        score = self._make_score()
        table = render_score_table(score)

        assert "运行时间长" not in table,     "运行时间长 不应出现"
        assert "至少=" not in table,          "至少= 不应出现"
        assert "恢复撤回" not in table,       "恢复撤回 不应出现"
        assert "运行时长惩罚" in table,       "应有'运行时长惩罚'"
        assert "越高=回撤控制越好" in table,  "应有正确说明"

    def test_none_total_shows_not_calculated(self):
        from backend.report_renderer import render_score_table
        score = self._make_score(total=None)
        table = render_score_table(score)
        assert "不计算正式综合分" in table

    def test_none_alpha_shows_not_applicable(self):
        from backend.report_renderer import render_score_table
        score = self._make_score(alpha=None)
        table = render_score_table(score)
        assert "不适用" in table


class TestMetricTableText:
    """验证指标溯源表不再有脏值"""

    def test_no_shuju_yuanzhu(self):
        """None值显示数据缺失，不是数据援助"""
        from backend.report_renderer import add_row_fixed
        row = add_row_fixed("近1年收益", None, "%")
        assert "数据援助" not in row
        assert "数据缺失" in row

    def test_no_xianyouriqi_column(self):
        """列名是截止日期，不是现有日期"""
        from backend.report_renderer import render_metric_table
        from types import SimpleNamespace
        from datetime import date

        snap = SimpleNamespace()
        snap.unit_nav = SimpleNamespace()
        snap.unit_nav.value = 1.5
        snap.unit_nav.source = "akshare"
        snap.unit_nav.is_mock = False
        snap.unit_nav.as_of = date.today()
        n = SimpleNamespace(); n.value = "real"
        snap.unit_nav.nature = n

        for field in ['accumulated_nav', 'fund_size', 'fund_size_bn',
                      'return_since_inception', 'return_1y', 'return_3y',
                      'max_drawdown', 'max_drawdown_pct',
                      'benchmark_return', 'benchmark_return_pct',
                      'alpha', 'alpha_pct']:
            setattr(snap, field, None)

        table = render_metric_table(snap)
        assert "现有日期" not in table
        assert "截止日期" in table

    def test_no_嘲笑_in_source(self):
        """嘲笑不应出现在来源列"""
        from backend.report_renderer import fmt_source
        from types import SimpleNamespace

        m = SimpleNamespace()
        m.source = "嘲笑"
        result = fmt_source(m)
        assert "嘲笑" not in result
        assert result == "mock"


class TestCommentaryCleaning:
    """验证 commentary 中的结论标题被清除"""

    def test_llm_heading_stripped(self):
        """LLM生成的📌方案结论不应出现在最终报告"""
        from backend.report_renderer import _clean_commentary
        text = "业绩分析内容...\n📌方案结论：风险较高\n更多内容"
        result = _clean_commentary(text)
        assert "方案结论" not in result

    def test_理念结论_stripped(self):
        from backend.report_renderer import _clean_commentary
        text = "风险分析...\n📌理念结论：持续观察\n更多"
        result = _clean_commentary(text)
        assert "理念结论" not in result

    def test_normal_text_preserved(self):
        """正常文本不应被删除"""
        from backend.report_renderer import _clean_commentary
        text = "该基金自成立以来收益显著，风险控制能力较好。"
        result = _clean_commentary(text)
        assert "自成立以来收益" in result


class TestBadgeText:
    """验证 Badge 不含 LLM 幻觉词"""

    def test_badge_no_丰田结论(self):
        """丰田结论不应出现在任何输出"""
        from backend.output_guard import validate_report
        text = "## 一、数据质量说明\n⚠️ 部分模拟\n丰田结论显示为信息不足\n## 八、⚠️ 风险提示\n不构成任何投资建议\n核心指标溯源\n综合评分"
        ok, errors = validate_report(text)
        assert not ok
        assert any("丰田结论" in e or "禁用词" in e for e in errors)


class TestBenchmarkResolver:
    """验证 161725 和 270042 的基准识别"""

    def test_161725_白酒_not_沪深300(self):
        from backend.benchmark_resolver import resolve_benchmark
        b = resolve_benchmark(
            "招商中证白酒指数证券投资基金",
            fund_type="股票型",
            declared_benchmark="沪深300"
        )
        # 基准应解析为白酒指数
        assert b.name == "中证白酒指数"
        # 与声明的沪深300不匹配，应有警告
        assert not b.is_matched
        assert b.mismatch_warning is not None

    def test_270042_纳斯达克100_resolved(self):
        from backend.benchmark_resolver import resolve_benchmark
        b = resolve_benchmark(
            "广发纳斯达克100交易型开放式指数证券投资基金联接基金(QDII)",
            fund_type="QDII"
        )
        assert b.name == "纳斯达克100指数"
        assert b.is_matched


class TestOutputGuardNew:
    """验证新增的结论标题检测"""

    def test_方案结论_blocked(self):
        from backend.output_guard import validate_report
        text = (
            "## 一、数据质量说明\n真实\n"
            "## 三、核心指标溯源\nX\n"
            "## 四、综合评分\n📌方案结论：风险较高\n"
            "## 八、⚠️ 风险提示\n不构成任何投资建议"
        )
        ok, errors = validate_report(text)
        assert not ok

    def test_正确适配结论_passes(self):
        from backend.output_guard import validate_report
        text = (
            "## 一、数据质量说明\n真实\n"
            "## 三、核心指标溯源\nX\n"
            "## 四、综合评分\n📌 适配结论：谨慎关注\n"
            "## 八、⚠️ 风险提示\n不构成任何投资建议"
        )
        ok, errors = validate_report(text)
        # 适配结论合法不应被阻断
        rating_errors = [e for e in errors if "非法结论标题" in e or "非法评级" in e]
        assert not rating_errors, f"合法结论标题不应被阻断: {rating_errors}"
```

---

### 给 Claude Code 的完整指令

```
阅读MyFix/v1_5.md
请对 fundrag-agent 项目执行 V2.1 精准修复，
解决三份样本报告（161725/026211/270042）中剩余的8类问题。

## 修改文件清单（5个文件）

### 1. backend/report_renderer.py（最重要）

a) 替换 render_score_table() 函数：
   - "含运行时间长处罚" → "含运行时长惩罚"
   - "至少=恢复撤回控制比较好" → "越高=回撤控制越好"
   - alpha=None 时显示"不适用（基准为模拟数据）"

b) 替换 add_row()/add_row_fixed()：
   - metric=None 时返回"数据缺失"，不是"数据援助"

c) 新增/替换 fmt_source()：
   - 本地 DIRTY_SOURCE_FIX 字典："嘲笑"→"mock"，"计算"→"calculated"
   - 不依赖 normalize_source 抛异常，本地兜底

d) 修复 render_report() 中：
   - new_fund_banner 字符串：删除"约束已"，保持"已受运行时长约束"
   - "适合人群"只在模板末尾出现一次（第四章节内）

e) 新增 _clean_commentary() 函数：
   - 用正则去除 LLM 生成的「📌方案/理念/概念/建议/丰田结论：...」行
   - 调用 auto_fix_text() 清洗幻觉词
   - 在 render_report 的五/六/七章节调用此函数处理 commentary

f) 修复列名：表头"现有日期" → "截止日期"（已在 add_row_fixed 中修复）

### 2. backend/constants.py

在 BANNED_WORDS 追加：
  "方案结论", "理念结论", "概念结论", "丰田结论",
  "运行时间长", "至少=恢复", "恢复撤回控制", "营养不良",
  "通讯作者", "认知社区情感"

在 AUTO_FIX_MAP 追加：
  "运行时间长处罚": "运行时长惩罚"
  "至少=恢复撤回控制比较好": "越高=回撤控制越好"
  "方案结论": "适配结论"
  "理念结论": "适配结论"
  "概念结论": "适配结论"
  "丰田结论": "适配结论"
  "能承受大幅恢复": "能承受大幅回撤"
  "不统计显著性": "不具统计显著性"
  "约束已": "约束"

### 3. backend/output_guard.py

新增函数 check_heading_variants()：
  用正则检测「📌(方案|理念|概念|建议|丰田)结论」
  返回 error list

在 validate_report() 主函数末尾调用：
  errors.extend(check_heading_variants(text))

### 4. backend/data_fetcher.py

a) 累计净值（accumulated_nav）修复：
   - 单独调用 ak.fund_open_fund_info_em(symbol=code, indicator="累计净值走势")
   - 成功时用真实值，失败时设 None（绝对不写死 1.0）
   - source 字段写 "akshare"（英文）

b) benchmark_resolver 接入：
   - 在 fetch_fund_snapshot 内，基本信息获取后，调用：
     from backend.benchmark_resolver import resolve_benchmark
     benchmark_info = resolve_benchmark(name, fund_type, declared_benchmark)
   - 将结果赋值到 snapshot.benchmark
   - 注意：声明基准从 info.get("业绩比较基准") 或 info.get("跟踪标的") 获取

c) 所有 MetricSource 的 source 字段：
   - 构建前调用 normalize_source(src, allow_warning=True)
   - normalize_source 失败时降级为 "mock" 并记录 warning

### 5. frontend/app.py

a) render_data_quality_badge() 全量替换：
   - 文案完全硬编码（见修改清单代码）
   - partial 时："适配结论为「信息不足」"（不含任何LLM生成文本）

b) render_score_section() 修改：
   - 删除 st.info(f"👥 适合人群：...") 这行
   - 适合人群只在报告模板（final_report）里显示，不在此重复

### 6. 新增 tests/test_v21_regression.py
   - 包含修改清单中的所有测试用例

## 执行顺序
1. constants.py（最先，其他依赖它）
2. report_renderer.py（核心修复）
3. output_guard.py（检测变体标题）
4. data_fetcher.py（数据修复）
5. frontend/app.py（展示修复）
6. tests/test_v21_regression.py（验证）

## 验证命令
# 清缓存
find cache/ -name "*.json" -delete

# 运行新测试
pytest tests/test_v21_regression.py -v --tb=short

# 运行所有测试
pytest tests/ -v --tb=short

# 手动验证
python -c "
from backend.report_renderer import render_score_table, add_row_fixed
from types import SimpleNamespace

# 测试1: 评分表说明列
s = SimpleNamespace()
s.history_score=7.0; s.sentiment_score=6.0; s.risk_control_score=5.0
s.alpha_adjustment=0.3; s.total_score=6.5; s.confidence_label='高'
table = render_score_table(s)
assert '运行时间长' not in table
assert '至少=' not in table
print('✅ 评分表说明列修复验证通过')

# 测试2: 空值
row = add_row_fixed('近1年收益', None, '%')
assert '数据援助' not in row
assert '数据缺失' in row
print('✅ 空值显示验证通过')
"

## 修复后三份报告预期变化
270042: "嘲笑"→消失 | "概念结论"→消失 | 丰田结论Badge→消失
161725: "运行时间长处罚"→"运行时长惩罚" | "方案结论"→消失 | 基准→中证白酒指数
026211: "约束已"→消失 | "理念结论"→消失 | "数据援助"→"数据缺失"
```